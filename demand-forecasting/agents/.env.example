# Bedrock API Key Configuration
# Use AWS_BEARER_TOKEN_BEDROCK as per AWS documentation
# Format: ABSK prefix followed by base64 encoded credentials
AWS_BEARER_TOKEN_BEDROCK=your-bedrock-api-key-here
