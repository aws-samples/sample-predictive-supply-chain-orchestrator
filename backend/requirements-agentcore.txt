# AgentCore Runtime dependencies (pre-installed into bundle for arm64)
bedrock-agentcore==1.0.1
strands-agents==1.1.2

# Data processing (used by optimization engine + tools)
pandas==2.2.0
numpy==1.26.0
scipy==1.12.0
pydantic==2.10.0

# Logging
structlog==23.2.0

# Environment
python-dotenv==1.2.2
